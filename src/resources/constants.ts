import * as path from 'path';

export const RESOURCES_PATH = path.join(__dirname);
export const TEMP_RESOURCES_PATH = `${RESOURCES_PATH}/temp`;
export const JSON_STORAGE_FILE_PATH = `${RESOURCES_PATH}/temp/json-storage.json`;
